v0 = Hackily use D1 logs, as proposed by Chao-Yuan.
v1 = Put together a proper script. Reads a config file, loads in a model,
     passes a dummy image through it, prints activation maps for each layer.


